c# MongoDB Configuration
# For local development with Docker Compose, use:
# MONGODB_URI=mongodb://admin:adminpassword@localhost:27017/qr-platform?authSource=admin
# For production, use your MongoDB Atlas or other MongoDB connection string
MONGODB_URI=mongodb://admin:adminpassword@localhost:27017/qr-platform?authSource=admin

# NextAuth.js Configuration
# Generate a random secret for production: openssl rand -base64 32
NEXTAUTH_SECRET=change-this-secret-in-production-use-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Application URL (used for generating QR code scan URLs)
# In production, set this to your domain: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node Environment
# Options: development, production, test
NODE_ENV=development
